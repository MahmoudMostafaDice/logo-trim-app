# Railway: Vite output must exist before `npm start`, or Express stays in API-only mode.
# https://docs.railway.com/guides/config-as-code
[build]
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
